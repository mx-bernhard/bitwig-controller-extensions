<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.b3rnhard</groupId>
	<artifactId>pattern-tracker</artifactId>
	<packaging>jar</packaging>
	<name>pattern-tracker</name>
	<version>0.1</version>
	
	<repositories>
       <repository>
         <id>bitwig</id>
         <name>Bitwig Maven Repository</name>
         <url>https://maven.bitwig.com</url>
       </repository>
   </repositories>

	<properties>
		<kotlin.version>2.1.20</kotlin.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>com.bitwig</groupId>
			<artifactId>extension-api</artifactId>
			<version>22</version>
		</dependency>
		<dependency>
			<groupId>org.jetbrains.kotlin</groupId>
			<artifactId>kotlin-stdlib</artifactId>
			<version>${kotlin.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.jetbrains.kotlin</groupId>
				<artifactId>kotlin-maven-plugin</artifactId>
				<version>${kotlin.version}</version>
				<executions>
					<execution>
						<id>compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<sourceDirs>
								<sourceDir>${project.basedir}/src/main/kotlin</sourceDir>
							</sourceDirs>
						</configuration>
					</execution>
					<execution>
						<id>test-compile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>test-compile</goal>
						</goals>
						<configuration>
							<sourceDirs>
								<sourceDir>${project.basedir}/src/test/kotlin</sourceDir>
							</sourceDirs>
						</configuration>
					</execution>
				</executions>
				<configuration>
					<jvmTarget>1.8</jvmTarget>
					<compilerPlugins>
						<plugin>all-open</plugin>
					</compilerPlugins>
					<pluginOptions>
					</pluginOptions>
				</configuration>
				<dependencies>
					<dependency>
						<groupId>org.jetbrains.kotlin</groupId>
						<artifactId>kotlin-maven-allopen</artifactId>
						<version>${kotlin.version}</version>
					</dependency>
				</dependencies>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.8.0</version>
				<configuration>
					<optimize>true</optimize>
					<fork>true</fork>
					<source>1.8</source>
					<target>1.8</target>
					<encoding>UTF-8</encoding>
					<maxmem>1024m</maxmem>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>3.5.1</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<minimizeJar>true</minimizeJar>
							<artifactSet>
								<includes>
									<include>org.jetbrains.kotlin:kotlin-stdlib</include>
									<include>org.jetbrains.kotlin:kotlin-stdlib-common</include>
									<include>org.jetbrains:annotations</include>
								</includes>
							</artifactSet>
						</configuration>
					</execution>
				</executions>
			</plugin>
		
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<version>3.1.2</version>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
		
			<plugin>
				<groupId>com.coderplus.maven.plugins</groupId>
				<artifactId>copy-rename-maven-plugin</artifactId>
				<version>1.0</version>
				<executions>
					<execution>
						<id>rename-file</id>
						<phase>install</phase>
						<goals>
							<goal>copy</goal>
						</goals>
						<configuration>
							<sourceFile>${project.build.directory}/${project.build.finalName}.jar</sourceFile>
							<destinationFile>${project.build.directory}/PatternTracker.bwextension</destinationFile>
						</configuration>
					</execution>
				</executions>
			</plugin>

		</plugins>
	</build>

	<profiles>
		<profile>
			<id>deploy-to-bitwig</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<version>3.0.0</version> 
						<executions>
							<execution>
								<id>copy-bwextension</id>
								<phase>install</phase> 
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<target>
										<property environment="env"/>
										<!-- Attempt to set target.dir from system property -->
										<condition property="target.dir" value="${bitwig.scripts.dir}">
											<isset property="bitwig.scripts.dir"/>
										</condition>

										<!-- If system property was not set, attempt to set target.dir from environment variable -->
										<condition property="target.dir" value="${env.BITWIG_SCRIPTS_DIR}">
											<and>
												<not><isset property="target.dir"/></not> <!-- Only if target.dir is not already set by system property -->
												<isset property="env.BITWIG_SCRIPTS_DIR"/>
											</and>
										</condition>

										<!-- Fail if target.dir is still not set or is empty -->
										<fail message="BITWIG_SCRIPTS_DIR environment variable or bitwig.scripts.dir system property must be set and not empty.">
											<condition>
												<or>
													<not><isset property="target.dir"/></not>
													<equals arg1="${target.dir}" arg2="" trim="true"/>
												</or>
											</condition>
										</fail>

										<echo message="Target Bitwig Scripts Directory is set to: ${target.dir}"/>
										<mkdir dir="${target.dir}"/>
										<copy file="${project.build.directory}/PatternTracker.bwextension"
											  tofile="${target.dir}/PatternTracker.bwextension"
											  overwrite="true" />
										<echo message="Copied PatternTracker.bwextension to ${target.dir}"/>
									</target>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>

